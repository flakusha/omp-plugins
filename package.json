{
  "name": "omp-plugins",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Installable oh-my-pi (omp) plugin bundle — extensions, hooks, rules, and agent config scaffold.",
  "workspaces": [
    "plugins/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@oh-my-pi/pi-coding-agent": "17.3.2",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check plugins scripts && bash scripts/check-no-console.sh",
    "lint:fix": "biome check --write plugins scripts && bash scripts/check-no-console.sh",
    "format": "biome format --write plugins scripts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "check:rules": "bash scripts/check-rules-sync.sh",
    "check:regex": "bash scripts/check-regex-safety.sh",
    "check:ship": "bash scripts/check-shipment.sh",
    "verify": "bun run lint && bun run typecheck && bun run test && bun run check:rules && bun run check:regex && bun run check:ship",
    "install:test": "bash scripts/install.sh --target /tmp/omp-test"
  }
}